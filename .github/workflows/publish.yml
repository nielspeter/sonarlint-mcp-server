name: Publish to npm

on:
  push:
    tags:
      - 'v*'

permissions:
  id-token: write  # Required for OIDC trusted publishing
  contents: write  # Required for creating GitHub releases

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test:run -- --exclude tests/e2e/**

      - name: Build
        run: npm run build

      - name: Publish to npm (OIDC)
        run: npm publish

      - name: Extract version from tag
        id: extract_version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            ## Installation

            ```bash
            npx @nielspeter/sonarlint-mcp-server
            ```

            ## Configuration

            ### Claude Code

            ```bash
            claude mcp add --transport stdio sonarlint -- npx -y @nielspeter/sonarlint-mcp-server
            ```

            ### Claude Desktop

            Manually add to `claude_desktop_config.json`:

            ```json
            {
              "mcpServers": {
                "sonarlint": {
                  "command": "npx",
                  "args": ["-y", "@nielspeter/sonarlint-mcp-server"]
                }
              }
            }
            ```

            See [README.md](https://github.com/nielspeter/sonarlint-mcp-server#readme) for full documentation.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
